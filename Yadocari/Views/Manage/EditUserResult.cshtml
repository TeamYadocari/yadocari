@model bool?
@{
  ViewBag.Title = "ユーザーの編集";
}

<h2>@ViewBag.Title</h2>

<h3>@(Request.Params["UserName"])の編集に@((bool) Model ? @Html.Raw("成功") : @Html.Raw("失敗"))しました。</h3>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}